import { View, Text } from "react-native";

export default function BasketRecommendationScreen() {
    return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>BasketRecommendationScreen</Text>
      </View>
    );
  }